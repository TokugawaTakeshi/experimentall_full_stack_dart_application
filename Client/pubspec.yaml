name: my_first_flutter_application
version: 0.0.0
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  business_rules:
    path: ../BusinessRules

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
